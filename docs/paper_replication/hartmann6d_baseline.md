# Hartmann6D Replication Status

The canonical Hartmann6D benchmark page is now
[Hartmann6D Benchmark](../examples/hartmann6d_toy.md).

Use that page for:

- the current runnable route: a local Hartmann adaptation of
  `config/branin_botorch_toy.yaml`,
- the adaptable scaffold: `config/hartmann6d_toy.yaml`, and
- the current boundary between benchmark wiring and future replication work.

Replication caveat: the repository includes the Hartmann oracle and benchmark
structure, but does not yet include a committed current-schema Hartmann run
configuration or the paper's comparison assets.

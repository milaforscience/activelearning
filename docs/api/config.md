# Configuration Model

The configuration layer validates the YAML specification into Pydantic models and exposes a `build()` method on each component block to produce runtime objects.

## Class Reference

::: activelearning.config.ActiveLearningConfig
    options:
      show_source: false
      heading_level: 3
